local AimingSway = require(script.AimingSway)
local CameraRotationSway = require(script.CameraRotationSway)
local MouseSway = require(script.MouseSway)
local WalkTilt = require(script.WalkTilt)
local CameraKick = require(script.CameraKick)
return {
	AimingSway = AimingSway,
	CameraRotationSway = CameraRotationSway,
	CameraKick = CameraKick,
	MouseSway = MouseSway,
	WalkTilt = WalkTilt
	
}